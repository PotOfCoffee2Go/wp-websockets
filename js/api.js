(function () {
    "use strict";
    module.exports = {
            testit(url, db, cb);
        }
    }

    function testit(url, db, cb) {
        console.log('testit!');
        if (cb) cb(null, data); // error, data
    }

}

